# Requirements
Make sure to have these dependencies installed
- npm
- node 22.13.1

# DEV Environment Setup
- In the terminal, do npm install
- Go to /frontend
- in the terminal do
```bash
npm install
npm run dev
```
- Go to /server
- In the terminal do
```bash
npm install
node index.js
```

